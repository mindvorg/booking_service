CREATE TABLE IF NOT EXISTS "feedback_on_agent" (
	"id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
	"agent_id" INTEGER NOT NULL,
	"user_id" INTEGER NOT NULL,
	"text" VARCHAR(255) NOT NULL,
	PRIMARY KEY("id")
);
